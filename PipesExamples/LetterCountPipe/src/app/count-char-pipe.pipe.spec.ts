import { CountCharPipePipe } from './count-char-pipe.pipe';

describe('CountCharPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CountCharPipePipe();
    expect(pipe).toBeTruthy();
  });
});
